Adding users/groups (its the same code involved)
================================================

    >>> from node.ext.ldap import Users

    >>> users = Users(layer['props'], layer['ucfg'])
    >>> users.keys()
    [u'uid0', u'uid1', u'uid2']

    >>> from node.bbb import AttributedNode
    >>> vessel_for_new = AttributedNode()
    >>> vessel_for_new.attrs['login'] = 'newcn'
    >>> vessel_for_new.attrs['sn'] = 'newsn'
    >>> users['new'] = vessel_for_new

